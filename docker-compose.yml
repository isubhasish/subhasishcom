services:
  bot:
    build: .
    container_name: subhasish_compressor
    restart: unless-stopped
    stop_grace_period: 30s
    volumes:
      - .:/app
    environment:
      PYTHONUNBUFFERED: '1'
      TZ: 'Asia/Kolkata'